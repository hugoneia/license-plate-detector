import { Linking } from "react-native";

export default function PlateMapScreen() {
  // Esta pantalla ya no se usa. La navegación a mapas se realiza directamente con Google Maps.
  // Este componente se mantiene por compatibilidad de rutas.
  return null;
}
